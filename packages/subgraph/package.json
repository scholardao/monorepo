{
    "name": "@scholardao/subgraph",
    "version": "0.1.0",
    "scripts": {
      "codegen": "graph codegen",
      "build": "graph build",
      "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ scholardao",
      "create-local": "graph create --node http://localhost:8020/ scholardao",
      "remove-local": "graph remove --node http://localhost:8020/ scholardao",
      "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 scholardao"
    },
    "dependencies": {
      "@graphprotocol/graph-cli": "0.21.1",
      "@graphprotocol/graph-ts": "0.20.0"
    }
}
